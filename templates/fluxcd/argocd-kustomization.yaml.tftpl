apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization

metadata:
  name: argocd
  namespace: ${namespace}

spec:
  interval: ${interval}
  path: ${path}
  prune: true

  sourceRef:
    kind: GitRepository
    name: ${git_repo_path}

  wait: true
  timeout: ${timeout}
  